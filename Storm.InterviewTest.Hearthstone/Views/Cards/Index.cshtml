@model Storm.InterviewTest.Hearthstone.Core.Features.Cards.Models.ViewDataModel
<!-- I modified the object we receive from the controller -->
@{
    ViewBag.Title = "Home Page";
}

<div class="page-header">
    <h2>Cards (Total: @Model.CardsData.Count())</h2>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="row" style="margin-bottom: 10px">
            <div class="col-md-12">
                <form class="form-inline" method="GET">
                    <!-- Iterate over the heroes card to get the name as value and textcontent-->
                    <select name="hero" id="hero">
                        <option default value="">All Classes</option>
                        @foreach (var heroe in Model.HeroesData)
                        {
                            <option value="@heroe.PlayerClassText">@heroe.PlayerClassText</option>
                        };
                    </select>
                    <!-- End of creation of dropdown menu -->
                    <input class="form-control" type="search" name="q" id="q" value="" placeholder="search for cards..." />
                    <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                </form>
            </div>
        </div>
        <!-- calling the partial display component-->
        @{Html.RenderPartial("_CardsList", Model.CardsData); }
    </div>
</div>